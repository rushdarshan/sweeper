<Page x:Class="ScreenshotSweeper.Views.SettingsTab"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Settings">

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <StackPanel Margin="20">
            <TextBlock Text="Notifications" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <CheckBox x:Name="ShowDetectionNotif" Content="Show notification on detection" Margin="0,5,0,5"/>
            <CheckBox x:Name="ShowDeletionNotif" Content="Show notification on deletion" Margin="0,5,0,5"/>
            <CheckBox x:Name="PlaySoundNotif" Content="Play sound on notification" Margin="0,5,0,20"/>

            <TextBlock Text="Startup" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <CheckBox x:Name="LaunchOnStartup" Content="Launch on Windows startup" Margin="0,5,0,5"/>
            <CheckBox x:Name="StartMinimized" Content="Start minimized to tray" Margin="0,5,0,20"/>

            <TextBlock Text="Monitoring" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                <TextBlock x:Name="StatusLabel" Text="Status: Monitoring Active" Margin="0,0,20,0"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <Button x:Name="ToggleMonitoringBtn" Content="Pause Monitoring" Click="ToggleMonitoring"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                <Button Content="Save Settings" Click="SaveSettings" Margin="0,0,10,0"/>
                <Button Content="Reset to Defaults" Click="ResetSettings" Margin="0,0,10,0"/>
                <Button Content="Clear Tracked Files" Click="ClearTracked"/>
            </StackPanel>

            <TextBlock x:Name="SaveStatus" Text="" Margin="0,20,0,0" FontSize="14"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Page>
